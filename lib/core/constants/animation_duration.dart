const Duration pageOpacityAnimationDuration = Duration(milliseconds: 750);
const Duration gradientAnimationDuration = Duration(milliseconds: 1000);
const int opacityDelay = 200;

const Duration delayBetweenLighting = Duration(seconds: 2);